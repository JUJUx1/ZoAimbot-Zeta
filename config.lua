-- config.lua - USER SETTINGS
return {
    -- Aiming Settings
    MaxDistance = 500,
    Smoothness = 0.8,
    Prediction = true,
    
    -- Visual Settings  
    ESPColor = {255, 0, 0},
    TargetColor = {255, 255, 0},
    ESPTransparency = 0.5,
    
    -- Toggle Defaults
    DefaultAimbot = true,
    DefaultESP = true,
    DefaultTeamCheck = false,
    
    -- UI Settings
    UIPosition = {0.5, -150, 0.1, 0},
    UISize = {300, 400}
}
